/**
* Class for model rules
* for creating a `cake` object.
*/

export interface Cakes {
    id: number,
    flavour: string,
    makingDate: Date,
    expirationDate: Date,
    ingredients: Object
}
